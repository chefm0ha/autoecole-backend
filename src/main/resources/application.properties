# Database Configuration
spring.datasource.url = jdbc:mysql://localhost:3306/autoEcole_Lakraa?serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = Lakraa

# JPA Configuration
spring.jpa.hibernate.ddl-auto = none
server.port = 9090

# Liquibase Configuration
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml
spring.liquibase.enabled=true
spring.liquibase.drop-first=true
spring.liquibase.contexts=dev

# WhatsApp Configuration
whatsapp.enabled=false
whatsapp.api.url=https://graph.facebook.com/v18.0/YOUR_PHONE_NUMBER_ID
whatsapp.api.token=YOUR_WHATSAPP_API_TOKEN

# Notification Settings
notification.cleanup.days=30
notification.retry.attempts=3